name: Release
on:
  push: { tags: [ "v*" ] }
permissions:
  contents: write
jobs:
  release:
    uses: ./.github/workflows/common-build.yml
    with:
      python-versions: '["3.12"]'
      oses: '["ubuntu-latest"]' # or '["ubuntu-latest","macos-latest","windows-latest"]'
      publish: true

